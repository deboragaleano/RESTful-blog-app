<%- include("../partials/header") %>

<div class="ui main text container segment">
    <div class="ui huge header">Edit Comment</div>
    <form class="ui form" action="/workouts/<%= workout_id %>/comments/<%= comment._id %>?_method=PUT" method="POST">
      <div class="field">
        <label>Text</label>
        <input type="text" name="comment[text]" value="<%= comment.text %>" /> 
      </div>
      <button class="ui teal big button" type="submit">Submit</button>
    </form>
</div>

<%- include("../partials/footer") %>


