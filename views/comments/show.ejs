<%- include("../partials/header") %>

<div class="ui main text container segment">
    <div class="ui huge header">Add New Comment</div>
    <form class="ui form" action="/workouts/<%= workout._id %>/comments" method="POST">
      <div class="field">
        <label>Text</label>
        <input type="text" name="comment[text]" placeholder="Text">
      </div>
      <button class="ui teal big button" type="submit">Submit</button>
    </form>
</div>

<%- include("../partials/footer") %>